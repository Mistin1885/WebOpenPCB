// Re-export shim — canonical contour form + validation lives in shared/ (used
// by the command executor, the draw/edit tools, and DXF import).
// See src/shared/rendering/pcb/contour-validation.ts.
export * from "../../../../shared/rendering/pcb/contour-validation";
