{
  "$defs": {
    "Diagnostic": {
      "properties": {
        "code": {
          "title": "Code",
          "type": "string"
        },
        "severity": {
          "enum": [
            "error",
            "warning",
            "info"
          ],
          "title": "Severity",
          "type": "string"
        },
        "message": {
          "title": "Message",
          "type": "string"
        },
        "path": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Path"
        },
        "context": {
          "additionalProperties": true,
          "default": {},
          "title": "Context",
          "type": "object"
        }
      },
      "required": [
        "code",
        "severity",
        "message"
      ],
      "title": "Diagnostic",
      "type": "object"
    }
  },
  "description": "``GET /v1/{engine}/{jobId}``. ``result`` is present only in a terminal state that\nproduced one (a cancelled job may carry a partial envelope). ``diagnostics`` are the\nsubmit-time warning/info items \u2014 available immediately, and also merged into a completed\nresult's ``warnings[]``.",
  "properties": {
    "jobId": {
      "title": "Jobid",
      "type": "string"
    },
    "status": {
      "anyOf": [
        {
          "enum": [
            "queued",
            "running",
            "done",
            "failed",
            "cancelled"
          ],
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Status"
    },
    "error": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Error"
    },
    "diagnostics": {
      "default": [],
      "items": {
        "$ref": "#/$defs/Diagnostic"
      },
      "title": "Diagnostics",
      "type": "array"
    },
    "result": {
      "anyOf": [
        {
          "additionalProperties": true,
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Result"
    }
  },
  "required": [
    "jobId"
  ],
  "title": "JobStatusResponse",
  "type": "object"
}
